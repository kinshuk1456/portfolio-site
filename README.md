# Portfolio Site (Static + Content-Driven)

This repository is scaffolded for a static-hostable portfolio that renders content from `src/content/**`.

- Author in `src/`
- Run build scripts in `scripts/`
- Upload `dist/` to Hostinger

Next steps (coming): styling system, rendering logic, content examples.
